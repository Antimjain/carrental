# URL of the Carental API. Leave unset for local development
# (defaults to http://localhost:3001). Set it to the deployed API
# URL when hosting the frontend, for example:
# VITE_API_URL=https://carental-api.onrender.com
VITE_API_URL=
