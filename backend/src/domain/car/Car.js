class Car {
  constructor({ id, brand, model, stock, prices }) {
    this.id = id;
    this.brand = brand;
    this.model = model;
    this.stock = stock;
    this.prices = prices;
  }
}

module.exports = Car;
